=stylesheet_link_tag 'labels', media: "all"

.page
  -@empty_boxes.times do
    .box
  -@first_boxes.each do |box|
    =render 'box', box: box
.page-break

-@boxes.in_groups_of(10, false).each do |box_group|
  .page
    -box_group.each do |box|
      =render 'box', box: box
  .page-break
